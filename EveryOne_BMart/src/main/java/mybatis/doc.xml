<?xml version="1.0" encoding="UTF-8"?>

  <!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="doc">
	<select id = "getMember" resultType = "member">
		select * from member where id = #{id}
	</select>
	
	<update id="updateDoc">
		update document set dstatus=1
		where dnum=#{dnum}

	</update>

	<update id="updateEnter">
		update enter set dstatus=1
		where enum=#{enum}

	</update>

	<update id="updateReview">
		update review_check set dstatus=1
		where rnum=#{rnum}

	</update>
	
	<update id="updateState">
		update state set dstatus=1
		where snum=#{snum}

	</update>
</mapper>