<?xml version="1.0" encoding="UTF-8"?>

<!-- member.xml -->
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member.Member">
	<select id = "getBmartMember" resultType = "member.model.MemberBean">
		select * from member where id = #{id} and ownercode is null
	</select>

	<update id="updateMember">
		update member set password=#{password}, name=#{name}, phone=#{phone}, email=#{email} where id=#{id}
	</update>
	
	<update id="updateMemberImage">
		update member set image=#{image} where id=#{id}
	</update>
</mapper>
