<?xml version="1.0" encoding="UTF-8"?>


<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sale">


	<select id="getAriaChart" resultType="sale.model.OrderInfoBean">

		select to_char(orderdate,
		'yyyy-mm') as orderdate, sum(o.pamount*p.price) as
		totalPrice
		from orderInfo o inner
		join product p on o.pnum = p.pnum
		group by to_char(orderdate,
		'yyyy-mm')
		order by 1
	</select>





</mapper>          
     